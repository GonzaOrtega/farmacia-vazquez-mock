import { AccountView } from "@/components/pages/AccountView";

export const metadata = {
  title: "Mi cuenta — Farmacia Vázquez",
};

export default function CuentaPage() {
  return <AccountView />;
}
